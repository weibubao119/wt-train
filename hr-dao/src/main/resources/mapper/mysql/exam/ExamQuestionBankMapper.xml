<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dyys.hr.dao.exam.ExamQuestionBankMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dyys.hr.entity.exam.ExamQuestionBank">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="radio_count" property="radioCount" />
        <result column="multi_count" property="multiCount" />
        <result column="judge_count" property="judgeCount" />
        <result column="subjective_count" property="subjectiveCount" />
        <result column="is_deleted" property="isDeleted" />
        <result column="creator" property="creator" />
        <result column="create_date" property="createDate" />
        <result column="updater" property="updater" />
        <result column="update_date" property="updateDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, radio_count, multi_count, judge_count, subjective_count, is_deleted, creator, create_date, updater, update_date
    </sql>

</mapper>
